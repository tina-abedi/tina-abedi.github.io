<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Tina Abedi | Portfolio</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    :root {
      --accent: #2563eb;
      --bg: #f5f7fb;
      --card: #ffffff;
      --text: #1f2937;
      --muted: #6b7280;
    }

    * {
      box-sizing: border-box;
    }

    body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background: linear-gradient(135deg, #e0e7ff, #f5f7fb);
  color: var(--text);
}

section {
  margin-top: 50px;
  padding: 30px;
  background: #f9fafb;
  border-radius: 12px;
}


    .container {
      max-width: 900px;
      margin: 60px auto;
      padding: 40px;
      background: var(--card);
      border-radius: 16px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.08);
    }

    header {
      text-align: center;
      margin-bottom: 50px;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
    }

    .tagline {
      color: var(--accent);
      font-weight: 500;
    }

    section {
      margin-top: 50px;
    }

    .section-title {
      font-size: 1.5rem;
      margin-bottom: 20px;
      border-left: 4px solid var(--accent);
      padding-left: 12px;
    }

    .project-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }

    .project {
      padding: 20px;
      border-radius: 12px;
      border: 1px solid #e5e7eb;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .project:hover {
      transform: translateY(-4px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.08);
    }

    .project-title {
      font-weight: 600;
      margin-bottom: 8px;
    }

    .tech {
      font-size: 0.9rem;
      color: var(--muted);
    }

    a {
      color: var(--accent);
      text-decoration: none;
      font-weight: 500;
    }

    a:hover {
      text-decoration: underline;
    }

    footer {
      margin-top: 60px;
      text-align: center;
      font-size: 0.9rem;
      color: var(--muted);
    }
  </style>
</head>

<body>
  <div class="container">
    <header>
      <h1>Tina Abedi</h1>
      <p class="tagline">
        UCLA Class of 2026 | Cognitive Science & Statistics + Data Science
      </p>
    </header>

    <section>
      <h2 class="section-title">About Me</h2>
      <p>
        I’m a UCLA student interested in cognitive science, data analysis, and
        technology. I enjoy building projects that combine human behavior with
        data-driven insights and am currently expanding my skills in software
        development and analytics.
      </p>
    </section>

    <section>
      <h2 class="section-title">Projects</h2>

      <div class="project-grid">
        <div class="project">
          <div class="project-title">Project One</div>
          <p>
            A short description of what this project does and what problem it solves.
          </p>
          <p class="tech">HTML · CSS · JavaScript</p>
        </div>

        <div class="project">
          <div class="project-title">Project Two</div>
          <p>
            Another project description. Explain impact, data used, or results.
          </p>
          <p class="tech">Python · SQL</p>
        </div>
      </div>
    </section>

    <section>
      <h2 class="section-title">Contact</h2>
      <p>Email: your.email@example.com</p>
      <p>
        GitHub:
        <a href="https://github.com/yourusername" target="_blank">
          yourusername
        </a>
      </p>
      <p>
        LinkedIn:
        <a href="#" target="_blank">Your LinkedIn</a>
      </p>
    </section>

    <footer>
      © 2026 Tina Abedi
    </footer>
  </div>
</body>
</html>

